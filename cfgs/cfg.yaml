source: "allianz" # allianz or nrma

emb_model_name: "Cohere"  # "OpenAI" or "Cohere"

retriever:
  top_k: 5
  retriever_type: "vector"  # "vector "or "hybrid"

generator:
  llm_type: "OpenAI"  # OpenAI or finetuned

evaluator:
  data_file: "data/allianz/test_data.csv"